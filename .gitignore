# dependencies
node_modules/

# build output
.next/
out/
dist/
cdk.out/
*.tsbuildinfo

# environment
.env*
# — except the documented template, which holds names and ARNs, never a value.
!.env.local.example

# ralph loop working files (the user's, and the ledger)
.claude/*.local.md

# os
.DS_Store
